package ch07extends.book.sec06.package2;

import ch07extends.book.sec06.package1.A;

public class C {
    public void method() {
//        A a = new A();    // 다른 패키지에 있기 때문에 A의 protected 필드, 생성자, 메소드에 접근 불가
//        a.field = "value";
//        a.method();
    }
}
